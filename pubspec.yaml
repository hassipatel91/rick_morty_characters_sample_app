name: rick_morty_characters_sample_app
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  json_annotation: ^4.8.1
  dio: ^5.3.3
  retrofit: ^4.0.1
  equatable: ^2.0.5
  flutter_bloc: ^7.0.1
  get_it: ^7.6.4
  meta: ^1.9.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  retrofit_generator: '>=5.0.0 <6.0.0'
  build_runner: '>=2.3.0 <4.0.0'
  json_serializable: ^6.7.1

flutter:
  uses-material-design: true
  assets:
  - images/
